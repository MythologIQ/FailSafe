# REPO CONSTITUTION

**Status:** ACTIVE
**Version:** 1.0
**Authority:** ABSOLUTE

## Preamble: The Genesis Philosophy

This system is an **original composition of development excellence**.
It synthesis best-in-class patterns (DeepWisdom Strategy, BMAD Lifecycle, TACHES Identity) into a unified, superior whole. We do not copy; we evolve.

This document defines the immutable laws of this repository. The Agent (Antigravity/Gemini) must adhere to these rules without exception.

## I. The Prime Directives (Behavioral Protocols)

1.  **The "Git Status" Rule**: NEVER run destructive git commands (`checkout`, `reset`, `clean`) without first running `git status`. If uncommitted changes exist, STOP and ASK.
2.  **The "Read-Before-Write" Rule**: Before rewriting any file, READ existing content to preserve imports/exports.
3.  **The "Syntax Verification" Rule**: A tool failure (e.g., `dir` fail) does NOT mean a file is missing. Check syntax and retry.
4.  **The "Answer First" Rule**: If the user asks a question, answer it directly. Do not implement code unless explicitly asked.
5.  **The "Wait for Command" Rule**: If instructions imply a pause ("finish a thought"), STOP and ASK for confirmation.
6.  **The "Sentiment Neutrality" Rule**: Do not alter protocols, skip steps, or make assumptions based on perceived user emotion (eager, angry, etc.). Adhere to defined logic strictly.
7.  **The "Isolation" Rule**: Do not assume Context crosses Workspace Boundaries. "Memory" is weak; "File System" is strong. Trust the Mirror.

## II. The Tech Stack (Constraints)

- **Language**: TypeScript / Node.js (unless otherwise specified).
- **Styling**: Vanilla CSS (unless User requests Tailwind).
- **Documentation**: Markdown (GFM).

## III. The File Structure (The Map)

- `g:\MythologIQ\Genesis\` (Root)
  - `.gemini/` (The Cortex: Memory & Rules)
  - `.agent/workflows/` (The Engine: SOPs)
  - `src/` (Source Code - TBD)
  - `docs/` (Documentation)

## IV. The Dojo Protocols (The Katas)

- **KATA-001 (Entrance)**: Always run `read_file .agent/workflows/kata_entrance.md` at start.
- **KATA-002 (Audit)**: Verify work with `.agent/workflows/pre_commit_audit.md` before saving.
- **KATA-003 (Mirror)**: Verify Reality vs Memory.
- **KATA-006 (Shield)**: Git Safety is absolute.
- **KATA-007 (Focus)**: Read before Write.

## V. The Genesis Lifecycle

1.  **Analysis (Blueprint)**: `Design Documents/`
2.  **Planning (Spec)**: `.gemini/specs/`
3.  **Solutioning (Plan)**: `implementation_plan.md`
4.  **Implementation (Code)**: Agentic Builder Mode
5.  **Verification (Walkthrough)**: `walkthrough.md`

6.  **The Specification Rule**: No code changes > 50 lines allowed without an approved `.gemini/specs/*.md` file.
7.  **The Delta Rule**: All planning must explicitly define "Deltas" (Modify/New/Delete).
8.  **The Design Rule**: Major modules require a Blueprint.
